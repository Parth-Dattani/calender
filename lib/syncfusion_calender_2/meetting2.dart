import 'package:flutter/material.dart';

class Meeting2{
  Meeting2(
       this.eventName,
       this.from,
       this.to,
       this.background,
       this.isAllDay
   );

  // String event;
  // DateTime startDate;
  // DateTime endDate;
  // Color eventColor;
  // bool isAllDat;

  String eventName;
  DateTime from;
  DateTime to;
  Color background;
  bool isAllDay;
}